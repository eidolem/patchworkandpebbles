{{!< default}}
{{!-- The tag above means - insert everything in this file into the {body} of the default.hbs template --}}

{{!-- The off-canvas-wrapper class hides scroll bars when the sidebar is opened --}}
<div class="off-canvas-wrapper">
    {{!-- All of the content that gets shifted when the nav opens lives in here --}}
    <div class="off-canvas-content" data-off-canvas-content>
        {{!-- The header blog image with the blog title and description centered --}}
        {{!-- <div class="row full-width">
            <div class="column">
                <div id="header" class="main-header" style="background-image: url({{@blog.cover}})">
                    <div class="span-middle">
                        <h1 class="page-title">{{@blog.title}} </h1>
                        <h2 class="page-description">{{@blog.description}}</h2>
                    </div>
                </div>
            </div>
        </div> --}}

        {{navigation}}

        {{!-- Main container content --}}
        <div class="row">
            <div class="column small-10">
                {{!-- Holds all of the sliders for various tags --}}
                <main>
                    <div class="slider-container">
                        {{> "loop-latest-post"}}
                        {{> "loop-slider" tag="travel"}}
                        {{> "loop-slider" tag="travel"}}
                        {{> "loop-slider" tag="travel"}}
                    </div>
                </main>
            </div>

            {{>sidebar}}

        </div>

        {{>footer}}
    </div>
</div>
